import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    reactStrictMode: true,
    // If you want the compiler, use the top-level key (not experimental):
    // reactCompiler: true
};

export default nextConfig;
